# Ambiente de execução da aplicação; defina no painel da hospedagem como development, test, staging ou production.
APP_ENV=development
# URL pública canônica do frontend; obtenha após criar o domínio/deploy no provedor de hospedagem web.
APP_URL=http://bighead.fbr.news
# URL pública da API FastAPI; obtenha após publicar a API no provedor de containers/PaaS.
API_URL=http://bighead-api.fbr.news
# Origens autorizadas pelo CORS, separadas por vírgula; use os domínios públicos do frontend fornecidos pela hospedagem.
CORS_ORIGINS=http://bighead.fbr.news
# Nível mínimo dos logs da aplicação; escolha entre DEBUG, INFO, WARNING ou ERROR conforme o ambiente.
LOG_LEVEL=INFO

# URL do projeto Supabase; copie em Supabase Dashboard > Project Settings > API ou execute `supabase status` localmente.
SUPABASE_URL=https://supabase.fbr.news
# Chave pública usada por clientes; copie a Publishable key em Supabase Dashboard > Project Settings > API.
SUPABASE_PUBLISHABLE_KEY= onde eu posso pegar isso no Supabase Self hosted ? 
# Chave server-only com privilégios elevados; use a Secret key atual do Dashboard (ou a service_role legada, se aplicável) somente no secret manager da API/worker.
SUPABASE_SECRET_KEY= onde eu posso pegar isso no Supabase Self hosted ? 
# DSN de aplicação, preferencialmente via pooler; copie em Supabase Dashboard > Connect > Connection string > Transaction pooler.
DATABASE_URL= postgresql://postgres:[YOUR-PASSWORD]@127.0.0.1:5432/postgres  como editar isso corretamente ?
# DSN direto exigido hoje também pelo readiness da API, além de migrations/admin; copie em Dashboard > Connect > Direct connection, restrinja a rede e nunca exponha ao frontend.
DIRECT_DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@127.0.0.1:5432/postgres  como editar isso corretamente ?
# Nome do bucket privado de artefatos; crie/consulte em Supabase Dashboard > Storage ou pelas migrations do projeto.
STORAGE_BUCKET=bighead 
# Endpoint HTTPS obrigatório do scanner antimalware em staging/produção; obtenha no serviço de scanning homologado ou mantenha uploads em quarentena até configurá-lo.
MALWARE_SCANNER_URL= Onde gerar isso ?

NEXT_PUBLIC_SUPABASE_URL= vamos usar essa ou sem NEXT_PUBLIC ?
# Chave pública usada por clientes; copie a Publishable key em Supabase Dashboard > Project Settings > API.
NEXT_SUPABASE_PUBLISHABLE_KEY= vamos usar essa ou sem NEXT_PUBLIC ?
# Chave server-only com privilégios elevados; use a Secret key atual do Dashboard (ou a service_role legada, se aplicável) somente no secret manager da API/worker.
NEXT_SUPABASE_SECRET_KEY=vamos usar essa ou sem NEXT_PUBLIC ?
# DSN de aplicação, preferencialmente via pooler; copie em Supabase Dashboard > Connect > Connection string > Transaction pooler.
NEXT_DATABASE_URL= vamos usar essa ou sem NEXT_ ?


# DSN do Redis usado por filas e eventos; obtenha no provedor Redis gerenciado ou use o serviço local do docker-compose.
REDIS_URL= Onde gerar isso ? No mesmo serviço do Supabase ou do app 
# Prefixo lógico da fila ARQ; definido pela equipe para isolar aplicação e ambiente no mesmo Redis.
QUEUE_NAME=bighead:jobs
# Duração, em segundos, do lease de um job antes de poder ser retomado; valor definido pela operação conforme o maior tempo normal de execução.
JOB_LEASE_SECONDS=300

# Reserva para o Client ID do OAuth Google, ainda sem consumidor no código; crie em Google Cloud Console > APIs & Services > Credentials ao implementar o provider.
AUTH_GOOGLE_CLIENT_ID=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Reserva para o Client secret do OAuth Google, ainda sem consumidor; copie do mesmo OAuth client e guarde no secret manager quando a integração for implementada.
AUTH_GOOGLE_CLIENT_SECRET=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Reserva para o Client ID do OAuth Microsoft, ainda sem consumidor; crie em Microsoft Entra > App registrations ao implementar o provider.
AUTH_MICROSOFT_CLIENT_ID=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Reserva para o Client secret do OAuth Microsoft, ainda sem consumidor; gere em Entra > Certificates & secrets somente ao integrar o provider.
AUTH_MICROSOFT_CLIENT_SECRET=OPTIONAL_UNTIL_PROVIDER_SELECTED


# Reserva para o host SMTP transacional, ainda sem consumidor na aplicação; obtenha no provedor escolhido e configure também o SMTP do Supabase Auth.
SMTP_HOST=mail.facebrasil.com
# Reserva para a porta SMTP, ainda sem consumidor na aplicação; use a porta informada pelo provedor, normalmente 587/STARTTLS ou 465/TLS.
SMTP_PORT=465

SMTP_FROM=appsextou@facebrasil.com

SMTP_SECURE=true
# Reserva para o usuário SMTP, ainda sem consumidor na aplicação; copie da credencial SMTP do serviço quando a integração for implementada.
SMTP_USERNAME=appsextou@facebrasil.com
# Reserva para a senha SMTP, ainda sem consumidor na aplicação; obtenha no provedor e armazene somente no Supabase/secret manager.
SMTP_PASSWORD=liberdade2026


# Provedor LLM principal consumido pelo worker: hermes, openai, anthropic ou google.
LLM_PROVIDER_DEFAULT=hermes
# Provedor LLM de contingência; deve ser diferente do principal.
LLM_PROVIDER_FALLBACK=
# Nome exato do modelo principal no provedor selecionado.
LLM_MODEL_DEFAULT=hermes
# Nome exato do modelo de fallback no provedor selecionado.
LLM_MODEL_FALLBACK=
# Timeout global do gateway; cada skill ainda pode impor limite menor.
LLM_TIMEOUT_SECONDS=60

# Reserva ainda sem consumidor para o modelo de embeddings; copie da documentação ao implementar e mantenha compatível com a dimensão do banco.
EMBEDDING_MODEL=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Dimensão planejada do embedding, ainda sem adapter consumidor; confirme na documentação do modelo antes de indexar dados reais.
EMBEDDING_DIMENSION=1536

# Orçamento planejado de LLM em centavos, ainda sem adapter consumidor; defina com Produto/Finanças ao habilitar o provider.
LLM_MONTHLY_BUDGET_CENTS=500000
# Chave OpenAI server-only; obrigatoria apenas se OpenAI for principal ou fallback.
OPENAI_API_KEY=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Chave Anthropic server-only; obrigatoria apenas se Anthropic for principal ou fallback.
ANTHROPIC_API_KEY=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Chave Google Gen AI server-only; obrigatoria apenas se Google for principal ou fallback.
GOOGLE_GENAI_API_KEY=OPTIONAL_UNTIL_PROVIDER_SELECTED

# Integração Hermes Agent
HERMES_API_URL=https://hermes.fbr.news/
HERMES_API_KEY=ja peguei
#Onde gerar os parametros abaixo ?
HERMES_PROFILES_DIR=f:\Projetos\BigHead\.omc\hermes\profiles  
HERMES_DEFAULT_MODEL=hermes
HERMES_TIMEOUT_SECONDS=60

# Integração AnythingLLM RAG - 
ANYTHING_LLM_API_URL=http://rag.fbr.news
ANYTHING_LLM_API_KEY=ja peguei
ANYTHING_LLM_DEFAULT_WORKSPACE=rag-bighead
ANYTHING_LLM_TIMEOUT_SECONDS=60

# Flags de comportamento de conhecimento/RAG
KNOWLEDGE_BACKEND=anythingllm
KNOWLEDGE_BACKEND_REQUIRED=true

# Reserva ainda sem consumidor para a URL do CRM; obtenha na documentação do CRM somente ao implementar o adapter.
CRM_BASE_URL=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Reserva ainda sem consumidor para a chave do CRM; gere no painel de integrações somente ao implementar o adapter.
CRM_API_KEY=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Reserva ainda sem consumidor para enriquecimento; gere no fornecedor homologado somente ao implementar o adapter.
ENRICHMENT_API_KEY=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Reserva ainda sem consumidor para campanhas de e-mail; gere no fornecedor somente ao implementar o adapter.
EMAIL_PROVIDER_API_KEY=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Reserva ainda sem consumidor para publicação social; obtenha no app OAuth/API somente ao implementar o adapter.
SOCIAL_PUBLISHING_API_KEY=OPTIONAL_UNTIL_PROVIDER_SELECTED
# Reserva carregada pela API, mas sem wiring operacional global comprovado; gere com `openssl rand -hex 32` ao definir a estratégia de assinatura e guarde no secret manager.
WEBHOOK_SIGNING_SECRET=local_webhook_secret_123
 
# Reserva carregada pela config, mas sem inicialização Sentry comprovada; copie o DSN em Sentry > Project Settings > Client Keys quando fizer o wiring.
SENTRY_DSN=
# Endpoint pretendido para OTLP; obtenha no provedor APM ou use o collector local, validando o exporter antes de considerar telemetria ativa.
OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318
# Headers pretendidos do OTLP no formato `chave=valor`; copie do provedor APM quando o exporter estiver configurado, ou deixe vazio localmente.
OTEL_EXPORTER_OTLP_HEADERS=
# Nome lógico usado para identificar os serviços nos traces; definido pela equipe e diferenciado por aplicação/ambiente quando necessário.
OTEL_SERVICE_NAME=bighead-platform

# Reserva carregada pela config, sem uso criptográfico comprovado; gere 32+ caracteres com CSPRNG quando o fluxo for implementado e guarde no secret manager.
ENCRYPTION_KEY=supersecretencryptionkey32chars12
# Pepper secreto aplicado aos tokens do portal; gere com CSPRNG, por exemplo `openssl rand -hex 32`, e nunca persista junto aos hashes.
PORTAL_TOKEN_PEPPER=local_pepper_secret_123
# Validade do download assinado em segundos; defina pela política de segurança. Uploads assinados do Supabase permanecem fixos em duas horas.
SIGNED_URL_TTL_SECONDS=900

ACME_EMAIL=sergio@facebrasil.com 

REDIS_PASSWORD=Super1404@@@
